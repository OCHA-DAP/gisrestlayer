DEBUG = False
APP_PORT = 5000
MAX_FILE_SIZE_MB = 300
TIMEOUT_SEC = 120
LOGGING_CONF_FILE = 'logging.conf'

DB_NAME = 'test_gis'
DB_USER = 'test_gis'
DB_PASS = 'some_password'
DB_HOST = 'gisdb'
DB_PORT = '5432'

REDIS_HOST = 'gisredis'
REDIS_PORT = 6379
REDIS_DB = 11 # we want to avoid a collision if running on a normal stack

VERIFY_CKAN_SSL=False

TMP_DOWNLOAD_DIRECTORY = '/tmp'

# gisapi url
GIS_API_PATTERN = 'http://gisapi/services/tables/{table_name}'
# ckan url to push back the reply

# gispreviewbot's key
CKAN_API_KEY = 'THE_API_KEY'
# point to our ckan
CKAN_SERVER_URL = 'ckantest'

CHECKS_CONFIG_PATH = '/srv/gislayer/config/config.json'

# Base url need to build calls to CKAN API
CKAN_API_BASE_URL = 'http://ckantest/api/action'
